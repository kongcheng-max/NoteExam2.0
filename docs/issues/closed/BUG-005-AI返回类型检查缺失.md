# BUG-005: AI ???????????

**????**: 2026-06-29
**???**: P1
**??**: pp/backend/services/deepseek.py:84,100 + pp/backend/routers/exams.py:27
**??**: ???

## ????

_extract_json ?? JSON ?????????????????????? .get() ??????????

`python
# services/deepseek.py:84
result = self._extract_json(resp_text)
return result.get("knowledge_points", [])

# routers/exams.py:27
kp_list = await deepseek_service.extract_knowledge_points(note.content)
# kp_list ???? list????? str / None / dict
for kp in kp_list:  # ?? kp_list ? str???????
`

????generate_questions ?? 
esult.get("questions", []) ??????????

## ????

| AI ?? | kp_list ? | ?? |
|---------|-----------|------|
| {"knowledge_points": "????"} | ??? | or kp in "????" ????????????????? |
| {"knowledge_points": null} | []????? | ?? 500?BUG-003? |
| {"knowledge_points": {...}} | dict | or kp in {...} ?? keys |

## ??

- ??? 500 Internal Server Error
- ???????????
- ????????????????

## ????

? extract_knowledge_points ? generate_questions ????????

`python
async def extract_knowledge_points(self, note_content: str) -> list[dict]:
    resp_text = await self._call_api(messages, max_tokens=2048)
    result = self._extract_json(resp_text)
    kps = result.get("knowledge_points", [])
    if not isinstance(kps, list):
        raise ValueError("AI ????????????? list")
    return kps
`
